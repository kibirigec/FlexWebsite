specVersion: "v1beta"
serviceId: "flexweb"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flexweb-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
